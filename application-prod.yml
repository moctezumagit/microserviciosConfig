spring:
  application:
    name: Config-prod
eureka:
  instance:
    prefer-ip-address: true
  client:
    fetch-registry: true
    register-whit-eureka: true
    service-url:
      defaultZone: http://localhost:8761/eureka
      
